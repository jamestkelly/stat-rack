/**
 * Represents the available measurement units.
 * @enum {string}
 */
export enum MeasurementUnit {
  /**
   * Represents the metric system (e.g., kilograms, meters).
   */
  Metric = "metric",
  /**
   * Represents the imperial system (e.g., pounds, feet).
   */
  Imperial = "imperial",
}
